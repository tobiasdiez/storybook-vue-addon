// We need to export these as objects, not only as types for volar to pick them up
export { Stories, Story } from './core/components'

export interface Options {
  // define your plugin options here
}
